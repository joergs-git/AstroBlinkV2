import XCTest
import ImageDecoderBridge

final class ImageDecoderTests: XCTestCase {
    // Tests will be added once real test images are available at known paths
    func testPlaceholder() {
        XCTAssertTrue(true, "ImageDecoder package builds successfully")
    }
}
